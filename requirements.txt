#Ovdje se nalaze sve potrebne biblioteke i podaci koje treba instalirati za rad agenta
#Ispod je verzija ako unutar terminala instaliramo datoteke
              # pip install fastapi uvicorn
              # pip install fastf1
              # pip install scikit-learn
              # pip install numpy pandas matplotlib
              # pip install stable-baselines3[extra]
fastapi
uvicorn
fastf1
scikit-learn
pandas
matplotlib
numpy
stable-baselines3[extra]

#Instalacija: pip install -r requirements.txt
#Ovo isto intsaliraj: pip install "shimmy>=2.0"

#Nakon instalacije paketa ide:
  # 1.) python prepare_data.py --> priprema podataka, pokreće se 1 ili kod promjene utrke\vozača 
#prepare_data.py trebaš pokrenuti samo jednom, osim ako želiš osvježiti/stvoriti nove podatke.
  # 2.) python train.py -->treniranje agenta
  # 3.) pokretanje REST API-a
     # 3.1 pip install uvicorn -->intsalacija uvicorn-a
     # 3.2 uvicorn api:app --reload  -->pokretanje API-ja 
     # 3.2 python -m uvicorn api:app --reload -->vezija koja radi ako naredba iznad ne 
     # 3.3 pokreniti main.ipynb samo kliknes > u vrhu kod 1. bloka koda
# Možeš otvoriti još i Swagger UI gdje ,ožeš slati stanja agentu, ali nije nužno. Link: http://127.0.0.1:8000/docs
  # 4.) python predict_strategies.py --> za dodatne analize
# ili se samo pokrene main.py koja sve automatizira, ali to kada se istestira cijeli program\kod do kraja


# Primjeri za testiranje Swagger linka:
  # 1. Suha staza, dobre gume
      #{
      #"state": [20, 1, 12, 2, 1, 5, 91.2, 30.5, 30.1, 1.8, 2.2, 1, 30.0, 22.0, 1]
      #}
  # 2. Mokra staza, istrošene gume
      #{
      #"state": [5, 2, 22, 2, 1, 8, 100.5, 35.0, 34.0, 0.5, 1.0, 2, 23.0, 18.0, 2]
      #}
  # 3. Kišni uvjeti, agresivan stil
      #{
      #"state": [7, 3, 18, 1, 1, 4, 105.0, 36.5, 35.2, 0.8, 1.5, 1, 19.0, 16.0, 3]
      #}
  # 4. Vruća staza, medium compound
      #{
      #"state": [15, 1, 9, 1, 1, 3, 95.0, 31.0, 30.0, 2.5, 3.0, 0, 36.0, 28.0, 1]
      #}

